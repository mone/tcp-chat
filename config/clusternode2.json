{
  "localCluster": {
    "enabled": true,
    "forks": "auto"
  },

  "cluster": {
    "enabled": true,
    "upperLevelServerHost": "localhost",
    "upperLevelServerPort": 10001,
    "disconnectionPolicy": "stop"
  },

  "echoToSender": true,
  "port": 20000,
  "logFile": "server2.log"
}
